1) Select library folder (select library_name.cpp).
2) Select .h files folder (select h_file_name.h).
   Click "Add directory".
   You can clear all directories and select again.
3) Test.

Path to library is saved to "LibraryPath.txt".
Paths to .h files are saved to "hPath.txt".